import { vars } from "nativewind";
import { darkThemeTokens, lightThemeTokens } from "./theme-tokens";

export const lightThemeVars = vars(lightThemeTokens);

export const darkThemeVars = vars(darkThemeTokens);
